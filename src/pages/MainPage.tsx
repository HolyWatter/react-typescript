import React from "react";

const MainPage = () => {
  function newFunc(x: number, y: number): number {
    return x + y;
  }

  const oldFunc = (x: number, y: number): void => {
    x + y;
  };

  return <div>asdfd</div>;
};

export default MainPage;
